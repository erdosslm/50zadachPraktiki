//16
#include<iostream>
#include<cmath>
using namespace std;
int main() {
	int  a, b, c, s1, s2, s3;
	cin >> a >> b >> c;
	s1 = a * 2;
	s2 = b - 3;
	s3 = pow(c, 2);
		cout << s1 + s2 + s3 << endl;
	return 0;
}